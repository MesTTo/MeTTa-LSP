/* eslint-disable boundaries/dependencies -- Compatibility shim: legacy adapters import this file while the authoritative registry lives in language-service. */
export * from "../language-service/capabilities.js";
